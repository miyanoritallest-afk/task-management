# 非機能要件

## 1. パフォーマンス

| 項目 | 要件 |
|------|------|
| 初期表示速度 | ページロードから表示完了まで2秒以内 |
| 操作レスポンス | カード追加・移動・削除の反映は即時（楽観的更新を使用） |
| カード上限 | 1カラムあたり最大100枚を想定（それ以上は動作保証外） |

---

## 2. 対応ブラウザ

| ブラウザ | バージョン |
|---------|-----------|
| Google Chrome | 最新版 |
| Microsoft Edge | 最新版 |
| Firefox | 最新版 |
| Safari | 最新版（macOS） |

モバイルブラウザは動作確認対象外とする。

---

## 3. 対応デバイス・レイアウト

| 項目 | 要件 |
|------|------|
| 対象デバイス | PCブラウザのみ（スマートフォン・タブレット非対応） |
| 最小画面幅 | 1024px以上を推奨 |
| レイアウト | 3カラム横並び固定。1024px未満では横スクロールで対応 |

---

## 4. データ永続化・セキュリティ

| 項目 | 要件 |
|------|------|
| 保存場所 | PostgreSQL（Docker Compose でローカル起動）。Spring Boot REST API 経由でアクセス |
| ORM | Spring Data JPA + Hibernate を使用してデータアクセスを型安全に管理 |
| マイグレーション | Flyway によるバージョン管理（`db/migration/V*.sql`） |
| セキュリティ | データベース接続情報は `application.properties` または環境変数で管理し、クライアントに露出させない |
| XSS対策 | ユーザー入力はテキストとして扱い、HTMLとして評価しない |
| デプロイ | ローカル開発環境のみ対象（Docker Compose + Gradle bootRun + Vite dev server） |

---

## 5. アクセシビリティ

| 項目 | 要件 |
|------|------|
| キーボード操作 | Escキーでモーダル・入力欄を閉じられること |
| フォーカス管理 | モーダル表示時はフォーカスをモーダル内に制限する |
| カラーコントラスト | テキストと背景のコントラスト比はWCAG AA基準（4.5:1以上）を目標とする |
